@model TestViewModel
@foreach (IngredientLine i in Model.RecipeIngredients)
{
    <a class="btn btn-block btn-secondary"
       asp-action="Index"
       asp-controller="Home"
       asp-route-category="@i"
       asp-route-productPage="1">
        @i.IngredientID
    </a>
}
    <p>@ViewBag.test</p>